[build]
  command = "./setup.sh"
  publish = "output"

[build.environment]
  # Match .python-version and pyproject.toml before setup.sh runs.
  PYTHON_VERSION = "3.12"
  PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
